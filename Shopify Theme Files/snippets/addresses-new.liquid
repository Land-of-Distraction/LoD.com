<div id="AddressNewForm" class="address-form hide">
  {% form 'customer_address', customer.new_address %}
  <div class="form-fields form-lod">
    <h2>{{ 'customer.addresses.add_new' | t }}</h2>
    <label for="AddressFirstNameNew" class="label-hidden">
        {{ 'customer.addresses.first_name' | t }}
    </label>
    <input  type="text"
            name="address[first_name]"
            class="input-field"
            id="AddressFirstNameNew"
            value="{{ form.first_name }}"
            required="required"
            placeholder="{{ 'customer.addresses.first_name' | t }}">

    <label for="AddressLastNameNew" class="label-hidden">
      {{ 'customer.addresses.last_name' | t }}
    </label>
    <input type="text"
            name="address[last_name]"
            class="input-field"
            id="AddressLastNameNew"
            value="{{ form.last_name }}"
            placeholder="{{ 'customer.addresses.last_name' | t }}">

    <label for="AddressCompanyNew" class="label-hidden">
      {{ 'customer.addresses.company' | t }}
    </label>
    <input type="text"
            name="address[company]"
            class="input-field"
            id="AddressCompanyNew"
            value="{{ form.company }}"
            placeholder="{{ 'customer.addresses.company' | t }}">

    <label for="AddressAddress1New" class="label-hidden">
      {{ 'customer.addresses.address1' | t }}
    </label>
    <input type="text"
            name="address[address1]"
            class="input-field"
            id="AddressAddress1New"
            value="{{ form.address1 }}"
            required="required"
            placeholder="{{ 'customer.addresses.address1' | t }}">

    <label for="AddressAddress2New" class="label-hidden">
      {{ 'customer.addresses.address2' | t }}
    </label>
    <input type="text"
            name="address[address2]"
            class="input-field"
            id="AddressAddress2New"
            value="{{ form.address2 }}"
            placeholder="{{ 'customer.addresses.address2' | t }}">

    <label for="AddressCityNew" class="label-hidden">
      {{ 'customer.addresses.city' | t }}
    </label>
    <input type="text"
            name="address[city]"
            class="input-field"
            id="AddressCityNew"
            value="{{ form.city }}"
            placeholder="{{ 'customer.addresses.city' | t }}">

    <label for="AddressCountryNew" class="label-hidden">
      {{ 'customer.addresses.country' | t }}
    </label>
    <div class="select-input">
      <select
        name="address[country]"
        class="input-field"
        id="AddressCountryNew"
        data-default="{{ form.country }}">
        {{ country_option_tags }}
      </select>
    </div>

    <div id="AddressProvinceContainerNew" class="select-input">
      <label for="AddressProvinceNew" class="label-hidden">
        {{ 'customer.addresses.province' | t }}
      </label>
      <select
        name="address[province]"
        class="input-field"
        id="AddressProvinceNew"
        data-default="{{ form.province }}">
      </select>
    </div>

    <label for="AddressZipNew" class="label-hidden">
      {{ 'customer.addresses.zip' | t }}
    </label>
    <input type="text"
            name="address[zip]"
            class="input-field"
            id="AddressZipNew"
            value="{{ form.zip }}"
            required="required"
            autocapitalize="characters"
            placeholder="{{ 'customer.addresses.zip' | t }}">

    <label for="AddressPhoneNew" class="label-hidden">
      {{ 'customer.addresses.phone' | t }}
    </label>
    <input type="tel"
            name="address[phone]"
            class="input-field"
            id="AddressPhoneNew"
            value="{{ form.phone }}"
            pattern="[0-9\-]*"
            placeholder="{{ 'customer.addresses.phone' | t }}">

    <label for="address_default_address_new" class="address-cta checkbox">
      {{ form.set_as_default_checkbox }}
      <div class="check-mark"></div>
      {{ 'customer.addresses.set_default' | t }}
    </label>

  </div>

  <div class="account-actions">
    <input type="submit" class="cta-white" value="{{ 'customer.addresses.add' | t }}">
    <button type="button" class="address-new-toggle cta-gray">
        {{ 'customer.addresses.cancel' | t }}
    </button>
  </div>

  {% endform %}

</div><!-- #AddressNewForm -->